{
  "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
  "width": "container",
  "height": 360,
  "data": {
    "url": "gabs_hottest_100_2025.json"
  },
  "transform": [
    {
      "calculate": "indexof(datum.status, 'Up ') == 0 ? toNumber(substring(datum.status, 3)) : indexof(datum.status, 'Down ') == 0 ? -toNumber(substring(datum.status, 5)) : 0",
      "as": "RankChange"
    },
    {"filter": "datum.RankChange != 0"},
    {
      "aggregate": [{"op": "sum", "field": "RankChange", "as": "NetRankChange"}],
      "groupby": ["style"]
    },
    {
      "calculate": "abs(datum.NetRankChange)",
      "as": "AbsNetRankChange"
    },
    {
      "window": [{"op": "rank", "as": "MoveRank"}],
      "sort": [{"field": "AbsNetRankChange", "order": "descending"}]
    },
    {"filter": "datum.MoveRank <= 12"},
    {
      "calculate": "datum.NetRankChange > 0 ? 'Gaining Popularity' : 'Losing Momentum'",
      "as": "Sentiment"
    }
  ],
  "mark": {"type": "bar", "cornerRadiusEnd": 4},
  "encoding": {
    "x": {
      "field": "style",
      "type": "nominal",
      "sort": {"field": "NetRankChange", "order": "descending"},
      "axis": {"labelFont": "Montserrat", "title": null, "labelAngle": -30, "labelLimit": 80}
    },
    "y": {
      "field": "NetRankChange",
      "type": "quantitative",
      "axis": {"labelFont": "Montserrat", "titleFont": "Montserrat", "title": "Net ranks gained / lost", "gridColor": "#E5E7EB"}
    },
    "color": {
      "field": "Sentiment",
      "type": "nominal",
      "scale": {
        "domain": ["Gaining Popularity", "Losing Momentum"],
        "range": ["#D97706", "#7C2D12"]
      },
      "legend": null
    },
    "tooltip": [
      {"field": "style", "type": "nominal", "title": "Style"},
      {"field": "NetRankChange", "type": "quantitative", "title": "Net Rank Change"}
    ]
  },
  "config": {
    "view": {"stroke": "transparent"},
    "font": "Montserrat"
  }
}
